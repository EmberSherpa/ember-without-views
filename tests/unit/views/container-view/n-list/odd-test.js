import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:container-view/n-list/odd', 'ContainerViewNListOddView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
