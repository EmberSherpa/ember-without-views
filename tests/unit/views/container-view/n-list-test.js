import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:container-view/n-list', 'ContainerViewNListView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
