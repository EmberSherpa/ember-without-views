import {
  setupTipper
} from 'ember-without-views/helpers/setup-tipper';

module('SetupTipperHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = setupTipper(42);
  ok(result);
});
