import {
  resetScrollTop
} from 'ember-without-views/helpers/reset-scroll-top';

module('ResetScrollTopHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = resetScrollTop(42);
  ok(result);
});
